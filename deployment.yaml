apiVersion: apps/v1
kind: Deployment
metadata:
  name: transformation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transformation
  template:
    metadata:
      labels:
        app: transformation
    spec:
      containers:
      - name: transformation
        image: transformation:latest
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - name: input-data
          mountPath: /app/input_data.csv
        - name: output-data
          mountPath: /app/output_data.csv
      volumes:
      - name: input-data
        hostPath:
          path: /data/input_data.csv
          type: File
      - name: output-data
        hostPath:
          path: /data/output_data.csv
          type: File
